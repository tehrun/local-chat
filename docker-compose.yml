services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    depends_on:
      mysql:
        condition: service_healthy
    environment:
      CHAT_DB_DRIVER: mysql
      CHAT_DB_HOST: mysql
      CHAT_DB_PORT: 3306
      CHAT_DB_NAME: local_chat
      CHAT_DB_USER: local_chat
      CHAT_DB_PASS: local_chat
    ports:
      - "0.0.0.0:8080:8080"
    volumes:
      - app_storage:/var/www/html/storage
    restart: unless-stopped

  mysql:
    image: mysql:8.0
    environment:
      MYSQL_DATABASE: local_chat
      MYSQL_USER: local_chat
      MYSQL_PASSWORD: local_chat
      MYSQL_ROOT_PASSWORD: root
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "127.0.0.1", "-uroot", "-proot"]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 30s
    volumes:
      - mysql_data:/var/lib/mysql
    restart: unless-stopped

volumes:
  app_storage:
  mysql_data:
